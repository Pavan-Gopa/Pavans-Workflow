# Live orchestration state — Orchestrator owns this file.
# Keep it simple. Project-agnostic template.

project_prefix: omp-dust
onboarding:
  status: complete
  model_pairs_confirmed: true
  completed_at: "2026-08-11T12:07:00Z"
current_step: S1
# Exact STEPS.md Do text while one item is actively assigned; Main owns/clears it.
current_work_item: null
step_description: >-
  Repair the global Dust provider's login path so workspace and US/EU region
  are validated and persisted instead of relying on brittle hard-coded values.
target_files:
  - /Users/pavan/.omp/agent/extensions/dust-provider.ts
completed_steps:
  - S1
skipped_steps: []
implementation:
  # pending | running | waiting_review | changes_requested | blocked | complete
  status: complete
  attempts: 1
  # Increment only after evidence evaluates an implementation attempt.
  # Runtime/provider interruption alone is not an implementation attempt.
retry_guard:
  max_attempts_without_progress: 3
  # Consecutive verified failures for the current approach/failure signature.
  # First occurrence = 1; same approach + same failure increments; a new
  # approach/evidence/material failure signature starts a new count at 1.
  repeated_failure_count: 0
  last_failure_signature: null
  blocker: null
omp:
  active_agent: null
  active_role: null
  last_result_artifact: agent://DustLoginQA1
  model_failure:
    # none | awaiting_human | backup_authorized
    status: none
    role: null
    primary_agent: null
    failed_model: null
    evidence: null
    backup_agent: null
    human_instruction: null
    authorized_at: null
  interruption:
    # none | interrupted_no_changes | interrupted_partial | recovered_result | indeterminate
    status: none
    role: null
    agent: null
    changed_files: []
    evidence: null
    recorded_at: null
review:
  status: approved
  last_feedback_file: AI_Workflow_Kit/docs/AI/FEEDBACK.md
  verdict: APPROVED
  note: Judgment Gates satisfied; Main verified the 404-only resolver at lines 282-300.
  # default on unless Human explicitly skips
  enabled: true
  # use status: skipped only after an explicit Human opt-out; record why in note
qa:
  status: qa_green
  last_report: AI_Workflow_Kit/docs/AI/REPORT.md
  suite: Dust provider login and routing smoke
  bugs_open: 0
  note: 5 passed, 0 failed; encoded, legacy, 401, and exhausted-404 paths verified.
  # recommended on; Human may opt out
  enabled: true
  # use status: skipped only after an explicit Human opt-out; record why in note
security:
  next_run: none
  note: >-
    Offer once near release when work is ready (optional). Top models only:
    GLM 5.2 max, Sol max, or Opus 5 max. Not every step. Not Terra/Luna.
next_actor: orchestrator
track: BUGFIX
plan_files:
  - AI_Workflow_Kit/docs/STEPS.md
  - AI_Workflow_Kit/docs/DECISIONS.md
  - AI_Workflow_Kit/docs/PROJECT_CONTEXT.md
checkpoint:
  last_pre_tag: null
  last_post_tag: null
  last_commit: null
coder_brief: |
  Repair Dust provider credential persistence and workspace/region discovery.
  Preserve raw-key compatibility and never expose live credentials.
notes: |
  Observed failure: /login dust validates the key against one hard-coded
  workspace/base URL and surfaces Dust API 404. Official Dust OpenAPI exposes
  both https://dust.tt and https://eu.dust.tt; 404 means workspace not found.
